import { Component } from "solid-js";

const TaskBar: Component<{}> = (props) => {
	return (
		<div class="w-screen h-16 bg-primary/30 backdrop-blur fixed bottom-0"></div>
	);
};

export default TaskBar;
